# Frontend environment — only PUBLIC, non-secret config is allowed here.
# Vite exposes only vars prefixed with VITE_ to the bundle (docs/security.md §8).
# Copy to `.env` (or `.env.local`) for local development.

VITE_API_BASE_URL=http://localhost:3000/api/v1
